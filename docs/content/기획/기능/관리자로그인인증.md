---
title: 관리자 로그인/인증
description: 관리자 화면 접근을 위한 공유 비밀번호 로그인과 세션 유지 기능
tags: [기능, 관리자, 완료]
aliases: []
created: 2026-09-06
updated: 2026-09-07
status: active
---

## 개요
`/admin` 접근 시 관리자 세션이 없으면 로그인 폼을 보여주고, 공유
비밀번호로 로그인하면 관리자 화면(메뉴 등록/삭제)을 쓸 수 있습니다.
역할 구분(점주/직원)은 이번 구현 범위에 포함하지 않았습니다 — "관리자
vs 비관리자" 한 단계만 가릅니다.

## 동작 방식
`AdminPage`가 마운트되면 `GET /api/admin/session`으로 현재 세션의
인증 여부를 확인합니다. 확인 중에는 로딩 문구, 미인증이면 로그인
폼(`LoginForm`)을, 인증되어 있으면 기존 메뉴 등록/삭제 화면과 로그아웃
버튼을 보여줍니다. 로그인 폼에서 비밀번호를 제출하면
`POST /api/admin/login`을 호출하고, 성공하면 관리자 화면으로
전환됩니다. 로그아웃 버튼은 `POST /api/admin/logout`으로 세션을
종료합니다.

## 관련 코드
- `apps/frontend/src/features/admin-auth` (`api/adminAuthApi.ts`,
  `model/store.ts`, `ui/LoginForm.tsx`)
- `apps/frontend/src/views/admin/ui/AdminPage.tsx`
- 서버 측 인증/인가 메커니즘은 [[관리자인증인가]] 참고

## 관련 문서
- [[기획서]]
- [[관리자인증인가]]
- `docs/specs/001-admin-auth` — 설계 결정과 구현 스펙
