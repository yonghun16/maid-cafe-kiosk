---
title: 메뉴 등록
description: 관리자 화면에서 "+ 메뉴 추가" 버튼을 누르면 뜨는 모달에서 이름/가격/이미지 파일/카테고리를 입력해 새 메뉴를 등록하는 기능
tags: [기능, 관리자, 완료]
aliases: []
created: 2026-09-06
updated: 2026-09-08
status: active
---

## 개요
관리자가 `/admin` "메뉴 관리" 탭의 **"+ 메뉴 추가" 버튼**을 누르면 뜨는
모달에서 메뉴 이름·가격·이미지·카테고리를 입력해 새 메뉴를 등록합니다.
이미지는 URL을 직접 입력하는 대신 파일을 선택하면 서버에 업로드되고,
반환된 URL이 자동으로 채워집니다.

처음엔 폼이 메뉴 목록 옆에 항상 펼쳐져 있는 형태였는데, 목록을 더 넓게
보여주고 화면을 덜 복잡하게 하기 위해 버튼 클릭 시 여는 모달
(`shared/ui/Modal`)로 바꿨습니다. 등록에 성공하면 모달이 자동으로
닫힙니다(`AddProductForm`의 `onSuccess` 콜백).

## 동작 방식
이미지 파일을 선택하면 즉시 백엔드의 `POST /api/uploads`로 업로드되어
Cloudflare R2에 저장되고, 반환된 공개 URL이 폼 상태에 채워지며 미리보기가
표시됩니다(업로드 중에는 "업로드 중..." 표시와 함께 제출 버튼이
비활성화됩니다). 이름·가격·이미지 중 하나라도 비어 있으면 안내 메시지가
뜨고 제출되지 않습니다. 등록에 성공하면 폼(이름·가격·이미지)이
초기화되고 메뉴 목록에 즉시 반영되며, 실패하면 폼 입력값은 유지된 채
에러 메시지만 뜹니다. 폼 제출 자체는 이미지 업로드로 받은 URL을 포함해
백엔드의 `POST /api/products`를 호출합니다.

## 관련 코드
- `apps/frontend/src/widgets/add-product-form`
- `apps/frontend/src/shared/ui/Modal.tsx` (범용 모달 — 다른 기능에서도
  재사용 가능)
- `apps/frontend/src/views/admin/ui/AdminPage.tsx` ("+ 메뉴 추가" 버튼과
  모달 열림 상태)
- `apps/frontend/src/entities/product` (`uploadProductImage`)
- `apps/frontend/src/features/product-management`
- `apps/backend` (`POST /api/uploads`, `POST /api/products`)

## 관련 문서
- [[기획서]]
