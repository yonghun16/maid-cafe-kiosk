---
title: 메이드 카페 키오스크 개발 계획서
description: 현재 아키텍처와 완료된 마일스톤, 다음 우선순위 로드맵을 정리한 개발 계획 문서
tags: [체크리스트, 로드맵]
aliases: [개발계획서, 로드맵]
created: 2026-09-05
updated: 2026-09-10
status: active
---

[[기획서]]에서 정의한 기능은 대부분 구현되어 있으며, 코드는 Turborepo
모노레포 + FSD(Feature-Sliced Design) 구조로 정리되어 있습니다. 백엔드
배포는 Render로 전환되었고 상품 이미지는 Cloudflare R2에 업로드하는
방식으로 바뀌었으며, 관리자 인증(공유 비밀번호 + 세션)도 추가됐습니다.
다음 우선순위는 결제 연동 여부 결정과 테스트 도입입니다.

## 현재 아키텍처

### 모노레포 구조
- `apps/frontend` — Next.js(App Router) 키오스크 화면
- `apps/backend` — Express + MongoDB REST API 서버
- `packages/types` — 프론트/백엔드가 공유하는 계약 타입(Product, Order,
  CreateOrderInput 등)
- `packages/ui`, `packages/eslint-config`, `packages/typescript-config` —
  공용 설정/컴포넌트

### 프론트엔드 (FSD)
`apps/frontend/src/`가 다음 레이어로 구성됩니다.
- `app` — Next.js 라우팅, 각 `page.tsx`는 `views/` 슬라이스를 조립만 함
- `views` — 라우트별 화면 조합 (`home`, `admin`, `kitchen`). 원래 FSD
  표준 명칭은 `pages`이지만, Next.js App Router가 `pages/` 디렉터리를
  레거시 Pages Router로 인식해 라우팅 충돌이 나기 때문에 이
  프로젝트에서는 `views`로 씁니다. `kitchen`은 `/kitchen` 라우트 —
  주문 확인/완료 처리만 담당하는 화면으로, `/admin`과 세션은 공유하되
  화면은 분리돼 있습니다(아래 마일스톤 참고).
- `widgets` — `order-type-select`(첫 화면의 매장/포장 선택),
  `product-list`, `order-summary`, `add-product-form`(이미지 파일을
  선택하면 백엔드에 업로드하고 반환된 R2 공개 URL을 미리보기와 함께
  씀), `manage-product-list`(`selectedCategory`로 필터링, 특정
  카테고리를 골랐을 때만 드래그 손잡이 + ▲▼ 순서 변경 UI 표시),
  `manage-category-list`("메뉴 관리" 탭 안의 카테고리 칩 바 — 클릭 시
  필터, 추가/인라인 수정/삭제/드래그(포인터 이벤트) 순서 변경 — ◀▶
  버튼은 뺌),
  `order-list`(주방 화면 "진행중 주문"/"지난 주문" 탭 — `status`
  prop으로 전환, 10초 폴링 + 수동 새로고침, 완료 버튼), `manage-ad-list`(광고
  관리 탭 — 썸네일 그리드 + 추가/수정 겸용 `AdForm`(모달), 드래그
  손잡이 + ◀▶ 순서 변경 UI), `sales-dashboard`(판매 통계 탭 — 연도
  선택 버튼(최근 5개년) + SVG 꺾은선 그래프로 그 해 월별 판매량 추이,
  점 클릭으로 고른 달의 메뉴별 판매 순위(데스크탑에서 2~3열 그리드).
  별도 상태 관리 없이 위젯 로컬 상태로 데이터 조회)
- `features` — `order-type`(매장/포장 선택 상태), `cart`(장바구니
  담기/주문 제출 — `makeCartItemId`로 상품 id+옵션 조합의 장바구니
  전용 식별자를 만들어, 같은 상품이라도 선택한 메뉴별 커스텀 옵션이
  다르면 다른 줄로 취급), `product-management`
  (메뉴 등록/수정/품절/재고 조정/
  삭제/순서 변경 — `adjustStock`, `moveProduct`/`reorderLocally`/
  `commitProductOrder`가 전부 같은 카테고리 안에서만 재배치),
  `category-management`(카테고리 추가/수정/삭제/순서 변경),
  `admin-auth`(관리자 로그인/로그아웃, 세션 확인),
  `ad-management`(광고 배너 추가/수정/삭제/순서 변경)
- `entities/product` — 상품 CRUD API, `ProductCard` UI(클릭 시 옵션
  선택 모달 — 매장 전체 고정 옵션 없이 `product.options` 체크박스만
  표시, [[옵션조합관리]] 참고), `ProductOptionsEditor` UI(메뉴별 자유
  옵션 이름+추가금 입력 줄 추가/삭제, 메뉴 추가/수정 폼 양쪽이 공유)
- `entities/category` — 카테고리 CRUD API(`getCategories`,
  `createCategory`, `updateCategory`, `deleteCategoryById`)
- `entities/order` — 주문 조회/완료 처리 API(`getOrders`,
  `completeOrder`)와 `OrderCard` UI, 판매 통계 API(`getMonthlySalesSummary`,
  `getProductSalesRanking`)
- `entities/ad` — 광고 CRUD API(`getAds`/`createAd`/`updateAd`/
  `deleteAdById`/`reorderAds`)와 고객 화면용 `AdBanner` UI(`order` 순
  자동 전환, 5초 간격, 광고 0개면 렌더링 안 함)
- `shared` — 공용 axios 인스턴스(`shared/api`), 상품/광고가 공용으로
  쓰는 이미지 업로드 함수(`shared/api/uploadImage`), API base URL 설정
  (`shared/config`), 도메인 지식 없는 범용 UI 컴포넌트(`shared/ui` — 현재
  `Modal`)

상태 관리는 Zustand(`features/cart`, `features/product-management`,
`features/category-management`, `features/ad-management`)를 사용합니다.

### 백엔드
- Express 5 + Mongoose, TypeScript로 작성
- REST 엔드포인트: `GET/POST /api/categories`,
  `PUT/DELETE /api/categories/:id`, `PATCH /api/categories/reorder`
  (`GET`은 공개, 나머지는 `requireAdmin`. 이름 수정/삭제 시 해당
  카테고리를 쓰는 상품도 함께 갱신/삭제),
  `GET/POST /api/products`(`GET`은 `category → order` 순 정렬),
  `PUT /api/products/:id`, `PATCH /api/products/reorder`(같은 카테고리
  상품 id 배열로 순서 재배열, `requireAdmin`),
  `PATCH /api/products/:id/sold-out`(품절 여부 토글),
  `PATCH /api/products/:id/stock`(재고 수량 절대값 설정 + 0 이하/초과에
  따른 자동 품절 처리/해제),
  `DELETE /api/products/:id`,
  `GET /api/orders`(최신순 조회, `status=pending|completed` 쿼리 필터,
  `requireAdmin`으로 보호), `PATCH /api/orders/:id/complete`(완료 처리),
  `POST /api/orders`(요청 바디에 매장 내/포장 구분 `orderType` 포함.
  성공 후 `decrementStockForOrder`로 재고 추적 상품만 자동 차감),
  `GET /api/orders/stats/monthly?year=YYYY`(그 해 1~12월 월별 판매량/
  매출 추이, 생략하면 올해, 없는 달은 0으로 채움), `GET
  /api/orders/stats/monthly/:month`(그 달 메뉴별 판매 순위, 판매량
  내림차순) — 둘 다 `requireAdmin`으로 보호, 완료 여부와 무관하게
  생성된 모든 주문을 집계,
  `GET/POST /api/ads`, `PUT/DELETE /api/ads/:id`, `PATCH
  /api/ads/reorder`(광고 id 배열로 순서 재배열)(`GET`은 공개, 나머지는
  `requireAdmin`. `GET`은 `order` 순 정렬),
  `POST /api/uploads`(이미지 파일을 Cloudflare R2에
  업로드하고 공개 URL 반환 — 상품/광고 이미지가 공용으로 씀),
  `GET /health`(DB에 접근하지 않는 가벼운
  헬스체크 — Render 자체 헬스체크와 무료 티어 슬립 방지용 외부 핑에 사용),
  `POST /api/admin/login`/`GET /api/admin/session`/`POST /api/admin/logout`
  (관리자 로그인/세션 확인/로그아웃)
- `packages/types`의 `CreateOrderInput`, `UploadImageResponse`,
  `AdminLoginInput`, `AdminSessionResponse`, `UpdateSoldOutInput`,
  `CategoryInput`, `ProductInput`, `ReorderProductsInput`, `Ad`,
  `AdInput`, `ReorderAdsInput`, `UpdateStockInput`, `MonthlySalesSummary`,
  `ProductSalesRanking`을 프론트엔드와 함께 사용해 요청/응답
  계약을 공유.
  `Product.category`는 고정 union(`'coffee'|'ade'|'dessert'`)이 아니라
  `string`으로 바뀌어 관리자가 등록한 어떤 카테고리 이름도 가리킬 수
  있음. `Product.order`(같은 카테고리 안에서의 순서)가 엔티티엔 있지만
  생성/수정 요청 바디엔 필요 없어서, 요청 바디 타입을 `Omit<Product,
  '_id'>` 대신 별도 `ProductInput`으로 분리함(`CategoryInput`과 같은
  패턴)
- Cloudflare R2 연동은 `src/lib/r2Client.ts`(`@aws-sdk/client-s3` 기반)에서
  담당하며, `multer`로 업로드된 파일을 메모리에 받아 R2로 전달
- 관리자 인증은 `express-session` + `connect-mongo`(기존 `MONGO_URI`
  재사용) 기반 세션이며, `src/middleware/requireAdmin.ts` 미들웨어가
  `POST/DELETE /api/products`, `POST /api/uploads`를 보호합니다. 세션
  쿠키를 프론트엔드(Vercel)와 주고받기 위해 CORS를 `FRONTEND_ORIGIN` 단일
  오리진 + `credentials: true`로 설정했습니다 — [[관리자인증인가]] 참고.

## 아키텍처 방향 메모 — 백엔드를 Next.js로 합치지 않고 분리 유지

**TL;DR**: 웹 키오스크뿐 아니라 향후 React Native 모바일 앱도 만들 계획이
있어([[기획서]] 범위 확장 시 반영 예정), `apps/backend`(Express)를
`apps/frontend`의 Next.js API Route로 흡수하지 않고 지금처럼 독립된
백엔드로 유지하기로 함. 지금 당장 처리할 작업은 아니고, 향후 구조를 정할
때 참고할 방향 메모.

**왜 이 논의가 나왔나**: `/admin` 관리자 인증을 만들며 프론트(Vercel)와
백엔드(Render)가 다른 오리진이라 CORS를 신경 써야 했던 걸 계기로, "차라리
백엔드를 Next.js API Route로 합쳐서 같은 오리진으로 만들면 더 간단하지
않을까"라는 질문이 나왔음.

**클라이언트가 웹 하나뿐이라면** Next.js API Route로 합치는 쪽이 실제로
더 단순했을 것(같은 오리진이라 CORS 자체가 불필요, 배포 대상도 Vercel
하나로 통합). 이 경우 `packages/types`, `packages/ui` 같은 공유 패키지와
Turborepo도 존재 의미가 줄어서 단일 Next.js 프로젝트로 단순화하는 것도
합리적인 선택지였음.

**하지만 모바일 앱 계획 때문에 결론이 바뀜**:
- React Native 앱도 결국 이 백엔드의 REST API를 호출해야 함. Next.js API
  Route도 기술적으로는 모바일에서 호출 가능하지만, 그러면 API가 "특정 웹
  프론트엔드 프로젝트의 부속물"이 되어 프론트 UI만 바꿔도 API가 같이
  재배포되는 등 결합이 생김. 웹/앱이 동등하게 의존하는 **독립된 백엔드
  서비스**로 두는 게 구조적으로 더 깔끔함.
- 앱이 웹(Next.js) + 모바일(React Native) + 백엔드(Express) 3개가 되면
  `packages/types`(공유 계약 타입)와 Turborepo 모노레포 구조가 다시
  확실한 존재 이유를 가짐 — 지금 구조를 그대로 확장하면 됨.
- **인증 방식 관련 참고**: 지금 관리자 인증(`express-session` 쿠키 기반,
  [[관리자인증인가]])은 매장 스태프가 웹으로만 쓰는 화면이라 문제없음.
  다만 앞으로 [[멤버십포인트조회]] 같은 **고객용 로그인**을 만들 때는
  웹/모바일 양쪽에서 써야 하므로, 쿠키 세션보다 토큰 기반(JWT를
  `Authorization` 헤더로 전달)이 React Native와 궁합이 좋음 — 브라우저가
  아닌 앱은 쿠키 자동 관리가 안 되기 때문. 그 기능을 실제로 만들 때
  다시 검토.

**결론**: 지금 구조(Turborepo 모노레포 + `apps/backend` 분리)를 유지한다.
실시간 주방 디스플레이(KDS, WebSocket 필요)도 서버리스보다 지속 연결이
가능한 별도 서버가 유리해서 같은 결론을 지지함. 모바일 앱 착수가
확정되면 `apps/mobile`을 추가하는 흐름이 될 것.

> 이 메모는 방향성 논의 요약이며, 실제로 모바일 앱 착수가 확정되는 등
> 구조가 확정되면 `docs/decisions/`에 정식 ADR로 남기는 것을 권장합니다
> (`docs/AGENTS.md` 참고).

## 완료된 마일스톤

- [x] 고객 메뉴 조회/필터링, 장바구니, 주문 제출 MVP 구현
- [x] 관리자 메뉴 등록/삭제 MVP 구현
- [x] Zustand 기반 상태 관리 도입
- [x] 프론트엔드 FSD 레이어 재구성, `entities/product`로 API 호출 중복 제거
- [x] 백엔드 JavaScript → TypeScript 전환, 타입 계약 공유
- [x] Cloudflare R2 이미지 업로드 도입 — 메뉴 등록 시 이미지 URL 직접 입력
  대신 파일 업로드 방식으로 전환
- [x] 백엔드 배포를 Railway에서 Render(무료 티어)로 전환, `GET /health`
  헬스체크 추가
- [x] 키오스크 화면을 핑크 메이드 카페 컨셉으로 리디자인 (상품 이미지
  3:4 세로 비율 적용 포함)
- [x] 장바구니 항목별 수량 개별 조절(+/-)·삭제 기능 추가 (기존에는 메뉴
  카드 재클릭으로 수량 증가만 가능했음)
- [x] 관리자 인증 추가 — 공유 비밀번호 기반 로그인/세션으로 `/admin`과
  메뉴 등록/삭제/이미지 업로드 API를 보호 (`docs/specs/001-admin-auth`)
- [x] 관리자 메뉴 수정 기능 추가 — 목록에서 바로 이름/가격/이미지/
  카테고리를 수정(`PUT /api/products/:id`, `requireAdmin`으로 보호)
- [x] 관리자 품절 처리 기능 추가 — 목록에서 클릭 한 번으로 품절/판매
  재개 토글(`PATCH /api/products/:id/sold-out`), 고객 화면에서는 품절
  메뉴를 흑백 처리하고 장바구니 담기를 막음([[메뉴수정품절처리]] 참고)
- [x] 운영 환경에서 관리자 세션 쿠키가 안 심기던 문제 수정 —
  `app.set('trust proxy', 1)` 추가. Render는 앞단 프록시(Cloudflare)가
  HTTPS를 종료하고 내부적으론 HTTP로 전달하는데, 이걸 신뢰하지 않으면
  Express가 연결을 안전하지 않다고 판단해 `cookie.secure: true`인 세션
  쿠키를 아예 Set-Cookie에 실지 않았음. 그 결과 로그인은 성공 응답이
  왔지만 이어지는 메뉴 수정/품절 처리가 전부 401로 실패했음
  ([[관리자인증인가]] 참고)
- [x] 매장 내(Dine-in)/포장(Takeout) 선택 추가 — 키오스크 첫 화면에서
  매장/포장을 먼저 선택해야 메뉴 목록으로 넘어가고, 선택값이
  `POST /api/orders`의 `orderType`으로 함께 저장됨. 주문 성공 시 다음
  손님을 위해 선택 화면으로 돌아감([[매장내포장선택]] 참고)
- [x] 테이블 번호 입력 기능은 필요 없다고 판단해 백로그에서 제거
- [x] 관리자 "주문 내역" 탭 추가 — `/admin`에서 들어온 주문을 최신순으로
  확인(10초 자동 갱신 + 수동 새로고침). 검색/필터, 주문 상태 변경은
  아직 없음([[주문내역조회]] 참고). 겸사겸사 `Order.items` 공유 타입이
  실제 저장 형태(`CartItem[]`)와 어긋나 있던 걸 발견해 `OrderItem`
  타입으로 바로잡음
- [x] 관리자 페이지 데스크탑 레이아웃 개선, 주문 항목에 메뉴 이미지
  스냅샷(`OrderItem.imageUrl`) 추가 — PC에서 주로 쓰는 화면인데 글자/
  이미지가 작았던 문제 개선
- [x] 주문 내역에 주문번호·날짜 표시 추가 — `Order.orderNumber`는
  MongoDB `_id` 대신 KST 기준 당일 자정부터 1번씩 다시 매기는 짧은
  번호(스타벅스 매장 주문번호 방식). 별도 카운터 컬렉션 없이 주문 생성
  시점에 오늘 자정 이후 주문 수를 세어 계산 — 동시 주문 시 번호가
  겹칠 수 있는 트레이드오프가 있음([[주문내역조회]] 참고)
- [x] 주문 완료 처리 추가 — `Order.isCompleted`(기본값 false)와
  `PATCH /api/orders/:id/complete`로 완료 처리하면 "진행중 주문" 탭에서
  빠지고 "지난 주문" 탭(`GET /api/orders?status=completed`)으로
  이동함. 접수/준비중처럼 더 세분화된 단계는 아직 없음
  ([[주문상태전이-API]] 참고)
- [x] 카테고리 관리 추가 — 고정 enum(`coffee`/`ade`/`dessert`)이던
  카테고리를 관리자가 `/admin`의 "메뉴 관리" 탭 안 카테고리 칩 바에서
  자유롭게 추가/수정/삭제할 수 있게 함. 상품은 카테고리를 id가 아니라
  이름 문자열로
  참조해서, 이름 수정 시 상품도 함께 갱신(`Product.updateMany`)되고
  삭제 시 소속 상품도 함께 삭제(`Product.deleteMany`)됨. 기존 상품
  데이터를 손대는 마이그레이션 없이, 서버 시작 시 `Category` 컬렉션이
  비어 있으면 `Product.distinct('category')`로 자동 시딩
  (`ensureDefaultCategories`)해 안전하게 전환함([[카테고리관리]] 참고)
- [x] 카테고리 순서 변경 기능 추가 — `Category.order`(숫자, 작을수록
  앞)와 `PATCH /api/categories/reorder`로 관리자가 ▲/▼ 버튼으로 순서를
  바꿀 수 있음. 이 순서는 고객 화면 카테고리 필터와 메뉴 등록/수정 폼의
  카테고리 목록에도 그대로 반영됨. `order` 필드가 없던 기존 카테고리는
  서버 시작 시 이름순으로 자동 부여(`ensureCategoryOrder`)
- [x] "메뉴 관리"와 "카테고리 관리" 탭 통합 — 별도 탭이던 카테고리
  관리를 "메뉴 관리" 탭 안의 카테고리 칩 바로 옮기고, 칩을 클릭하면
  그 카테고리의 메뉴만 필터링되게 함(`AdminPage`가 `selectedCategory`
  상태를 들고 `ManageCategoryList`/`ManageProductList`에 함께 전달).
  선택된 카테고리 이름을 바꾸면 필터도 따라가고, 선택된 카테고리를
  삭제하면 필터가 "전체"로 돌아감([[카테고리관리]] 참고)
- [x] 카테고리 칩 드래그 앤 드롭으로 순서 변경 — 처음엔 브라우저 네이티브
  HTML5 드래그(`draggable` 속성)로 구현했는데, 칩 안에 버튼이 여러 개
  겹쳐 있어 브라우저/기기에 따라 드래그 시작 자체가 인식되지 않는
  문제가 실사용에서 나옴(터치 기기는 애초에 미지원). **포인터 이벤트
  (mouse/touch/pen 통합) 기반 커스텀 드래그**로 다시 구현해 해결함 —
  칩 앞 전용 손잡이(⠿)에서 `pointerdown`으로 시작해 `window`의
  `pointermove`로 좌표 hit-test하며 화면만 즉시 재배치하다가
  (`reorderLocally`, API 호출 없음), `pointerup`에서 한 번만 서버에
  저장(`commitCategoryOrder`). ◀▶ 버튼도 대안으로 남겨둠([[카테고리관리]]
  참고)
- [x] 고객 화면 카테고리 필터에서 "전체" 옵션 제거 — 손님이 전체보기
  화면에서 실제로 메뉴를 고르지 않는다는 실사용 관찰에 따라, 메뉴를
  불러오면 항상 첫 번째 카테고리가 자동 선택되도록 바꿈
  (`ProductList.tsx`). 관리자 화면("메뉴 관리" 탭)의 "전체" 필터는
  전체 메뉴를 한눈에 보며 관리하는 워크플로우가 여전히 필요해 그대로
  유지함([[카테고리별메뉴조회]] 참고)
- [x] 관리자 메뉴 등록을 "+ 메뉴 추가" 버튼 + 모달 방식으로 변경 — 기존엔
  등록 폼이 메뉴 목록 옆에 항상 펼쳐져 있었는데, 목록을 더 넓게 보여주고
  화면을 덜 복잡하게 하려고 버튼 클릭 시 여는 모달로 바꿈. 범용
  `Modal` 컴포넌트를 `shared/ui`에 새로 만들어(배경 클릭/✕/ESC로 닫기)
  다른 기능에서도 재사용할 수 있게 함. `AddProductForm`은 카드
  껍데기(제목/배경)를 스스로 그리는 대신 `Modal`이 감싸도록 폼만 남기고,
  등록 성공 시 호출되는 `onSuccess` 콜백을 추가해 모달이 자동으로
  닫히게 함([[메뉴등록]] 참고)
- [x] 메뉴(상품)도 카테고리 순서 변경과 같은 방식으로 순서 변경 추가 —
  `Product.order`(카테고리 안에서만 의미 있음)와
  `PATCH /api/products/reorder`로, "메뉴 관리" 탭에서 **특정 카테고리를
  골랐을 때만**(전체 보기 중엔 카테고리가 섞여 있어 순서가 무의미해
  숨김) 드래그 손잡이(⠿)와 ▲▼ 버튼이 나타남. 카테고리 드래그 때 만든
  포인터 이벤트 기반 커스텀 드래그 패턴(손잡이 `pointerdown` → `window`
  `pointermove`로 hit-test하며 로컬 재배치 → `pointerup`에 한 번만 저장)
  을 그대로 재사용. 새 메뉴는 그 카테고리 안에서 맨 뒤 순서로 등록되고,
  `order` 필드가 없던 기존 메뉴는 서버 시작 시 카테고리별 생성순으로
  자동 부여(`ensureProductOrder`). `POST/PUT /api/products`의 요청
  타입을 `Omit<Product, '_id'>`에서 별도 `ProductInput`(카테고리
  `CategoryInput`과 같은 패턴)으로 분리해, `order`가 상품 엔티티엔
  필요하지만 생성/수정 요청 바디엔 필요 없는 것을 타입으로 표현함
  ([[메뉴수정품절처리]] 참고)
- [x] 메뉴 수정도 "+ 메뉴 추가"와 같은 이유로 모달 방식으로 변경 —
  기존엔 목록의 그 항목 자리가 그대로 편집 폼으로 바뀌는 인라인
  방식이었는데, 목록을 더 넓게 보여주고 화면을 덜 복잡하게 하려고
  `shared/ui/Modal`을 재사용하는 모달 방식으로 통일함. `EditProductForm`
  도 `AddProductForm`처럼 카드 껍데기(제목/배경)를 스스로 그리지 않고
  폼만 남김([[메뉴수정품절처리]] 참고)
- [x] 광고 관리 추가 — 관리자가 `/admin`의 새 "광고 관리" 탭에서 첫
  화면(매장/포장 선택 화면)에 노출할 광고 배너 이미지를 추가·수정·
  삭제. `Ad` 컬렉션(`{ imageUrl }`, `timestamps: true`)이 원본이며,
  순서 필드 없이 등록순(`createdAt` 오름차순) 그대로 노출됨. 고객
  화면에는 `AdBanner`가 5초 간격으로 자동 전환하며 보여주고, 광고가
  0개면 아무것도 렌더링하지 않음. 이 김에 상품 전용이던
  `uploadProductImage`를 도메인 지식 없는 `shared/api/uploadImage`로
  옮겨서 상품/광고 이미지 업로드가 같은 함수를 공유하게 정리함. 로컬
  에서 광고 생성/수정/삭제 전체 플로우를 curl로 검증(테스트 데이터는
  정리 완료)([[광고관리]] 참고)
- [x] 광고도 카테고리/메뉴와 같은 방식으로 드래그 순서 변경 추가 —
  `Ad.order` 필드와 `PATCH /api/ads/reorder`를 새로 추가하고(등록순
  `createdAt` 정렬에서 `order` 정렬로 전환), "광고 관리" 탭의 각 카드에
  드래그 손잡이(⠿) + ◀▶ 버튼을 붙임. 카테고리 드래그 때 만든 포인터
  이벤트 기반 커스텀 드래그 패턴(손잡이 `pointerdown` → `window`
  `pointermove`로 hit-test하며 로컬 재배치 → `pointerup`에 한 번만 저장)
  을 그대로 재사용. `order` 필드가 없던 기존 광고는 서버 시작 시
  등록순으로 자동 부여(`ensureAdOrder`). 로컬에서 순서 변경 API를
  curl로 검증할 때, 실제 운영 DB에 이미 있던 진짜 광고 2개의 `order`
  값(0, 1)은 건드리지 않고 테스트용 광고만 별도로 만들고 지워서 실제
  데이터에 영향이 없음을 확인함([[광고관리]] 참고)
- [x] 광고 배너를 매장/포장 선택 버튼 위로 옮기고 반응형으로 확대 —
  화면 너비에 따라 배너 크기가 단계적으로 커지도록 조정(너비 `max-w-sm
  → sm:max-w-xl → md:max-w-2xl → lg:max-w-4xl`, 높이 `h-56 → sm:h-72 →
  md:h-80 → lg:h-96`)([[광고관리]] 참고)
- [x] 재고 관리 추가 — "메뉴별 재고 수량 필드 추가 여부"가 열린
  질문이었는데, **상품마다 선택 적용**으로 결정함(`Product.stock`은
  선택 필드, 값이 없으면 지금처럼 재고 개념 없이 수동 품절 토글만
  적용). 값을 설정한 상품만 `POST /api/orders` 성공 후
  `decrementStockForOrder`가 주문 수량만큼 자동 차감하고, 0 이하가
  되면 자동으로 `isSoldOut: true`(고객 화면은 기존 품절 표시를 그대로
  재사용, 별도 UI 추가 없음). 관리자 목록에서 재고가 있는 상품에만
  "재고 N개" + ＋/－ 버튼이 나타나 즉시 조정 가능(`PATCH
  /api/products/:id/stock`, 절대값 설정이며 0보다 크게 재설정하면
  품절도 자동 해제 — 재입고 시나리오). 로컬에서 재고 차감/자동 품절/
  수동 재입고 전체 플로우를 curl로 검증(테스트 데이터 정리 완료)
  ([[재고관리]] 참고)
- [x] 판매 통계 대시보드 추가 — "조회할 지표 범위"가 열린 질문이었는데,
  **월별 추이 차트 + 선택한 달의 메뉴별 판매 순위**로 정함(일별/카테고리별
  집계는 범위 밖). `/admin`에 새 "판매 통계" 탭을 추가하고,
  `GET /api/orders/stats/monthly`(최근 6개월, 없는 달은 0으로 채움)와
  `GET /api/orders/stats/monthly/:month`(그 달 메뉴별 판매 순위)를
  MongoDB 실시간 집계(`$group`/`$unwind`)로 구현. 완료 여부와 무관하게
  생성된 모든 주문을 판매로 집계함(취소 개념이 없고 완료 처리는 매출과
  무관한 "주방 전달" 의미라서). KST 월 경계 계산은 기존
  `getKstStartOfToday`와 같은 패턴(`getLastNMonthLabels`/
  `getKstMonthRange`). 검증 과정에서 실제 운영 DB의 원본 주문 데이터를
  Python으로 직접 집계해 API 응답과 숫자가 정확히 일치하는지
  대조했고, 그 과정에서 이전 재고 기능 테스트 때 정리하지 못하고
  남아 있던 테스트 주문 2건(당시엔 카테고리 삭제로 상품만 지워지고
  주문 자체는 지우는 API가 없어 못 지웠음)을 발견해 별도 스크립트로
  직접 MongoDB에서 삭제, 실제 매출 통계에 섞이지 않게 정리함
  ([[매출통계대시보드]] 참고)
- [x] 판매 통계 메뉴별 순위의 이미지 안 뜨던 문제 수정 —
  `OrderItem.imageUrl`이 나중에 추가된 필드라 그 이전 주문 스냅샷엔
  값이 없어서(실제 데이터 대부분 해당) 발생. 없으면 현재 상품 정보의
  이미지로 대신 채우는 폴백을 추가했고, 처음 구현할 때 집계 결과의
  `ObjectId`를 문자열로 안 바꾸고 `Map` 키 조회에 써서 폴백이 항상
  실패하던 버그도 같은 자리에서 발견해 고침(`String(r._id)`). 상품이
  삭제돼 폴백도 없으면 프론트에서 회색 placeholder 박스로 대체
  ([[매출통계대시보드]] 참고)
- [x] 주방 화면(`/kitchen`)을 관리자 화면(`/admin`)에서 분리 — 메뉴/
  광고/통계 관리와 섞여 있으면 주문만 계속 지켜봐야 하는 주방/카운터
  직원 입장에서 산만하다고 판단해, "진행중 주문"/"지난 주문" 탭을
  `/admin`에서 떼어내 전용 라우트 `/kitchen`으로 옮김(`views/kitchen`,
  기존 `OrderList` 위젯 그대로 재사용). 로그인 세션은 `/admin`과 같은
  공유 비밀번호 기반 세션을 그대로 쓰므로(같은 오리진의 세션 쿠키
  공유) 이번 분리는 **화면 구성만** 나눈 것이고 접근 권한 차등은
  포함하지 않음 — 실제 역할별 권한 분리는 이미 백로그에 있는
  "관리자 역할 구분" 항목에서 별도로 다룸. 두 화면 헤더에 서로를 오갈
  수 있는 링크("주방 화면"/"관리자 화면")를 추가함([[주문내역조회]]
  참고)
- [x] 광고 배너 하단 흰 띠 제거 + 드래그로 넘기기 추가 — 순서 표시
  점을 이미지 아래 별도 흰 배경 띠 대신 이미지 위에 겹쳐(`absolute`)
  그리도록 바꿔 흰 띠를 없앰. 포인터 이벤트로 좌우 드래그(스와이프)를
  감지해 40px 이상 움직이면 다음/이전 광고로 전환(마우스/터치 공통,
  `touch-pan-y`로 세로 스크롤은 그대로 허용)([[광고관리]] 참고)
- [x] 관리자 카테고리 목록의 ◀▶ 순서 변경 버튼 제거 — "드래그만으로
  충분하다"는 판단에 따라 카테고리 칩에서만 버튼 액션을 빼고 드래그
  손잡이만 남김. 처음엔 이 요청을 메뉴(상품) 목록으로 잘못 이해해서
  메뉴의 ▲▼ 버튼을 지웠다가(커밋 되돌림), 카테고리가 맞다는 정정을
  받고서 `moveCategory`/`moveCategoryToIndex` 액션과 그 버튼 UI를
  제거함. 메뉴 목록의 ▲▼ 버튼과 광고의 ◀▶ 버튼은 이 결정과 무관하게
  그대로 유지([[카테고리관리]] 참고)
- [x] 판매 통계를 막대 그래프 → 꺾은선 그래프로 변경 + 연도 필터 추가
  — "최근 6개월" 고정 범위 대신, 연도 선택 버튼(올해~4년 전)으로 그
  해 1~12월 추이를 볼 수 있게 바꿈. 백엔드
  `GET /api/orders/stats/monthly`를 `months` 쿼리에서 `year` 쿼리로
  전환(`getKstYear`/`getMonthLabelsForYear` 헬퍼 추가, 상한
  `$lt: rangeEnd`도 새로 추가 — 과거 연도 조회 시 그 이후 데이터까지
  섞이지 않도록). 프론트는 외부 차트 라이브러리 없이 SVG로 직접
  꺾은선 그래프를 그림(`MonthlyLineChart`, `viewBox` 기반이라 반응형).
  관리자 화면은 주로 PC에서 쓴다는 기존 전제에 맞춰 그래프 높이와
  순위 목록(2~3열 그리드)도 데스크탑 위주로 더 넓게 키움. 로컬에서
  2025/2026/2024(데이터 없는 해) 세 연도를 curl로 교차 검증
  ([[매출통계대시보드]] 참고)
- [x] 상품 옵션 선택(샷 추가) 추가 — "메뉴 옵션 그룹을 어떻게 설계할지"
  가 열린 질문이었는데, **옵션은 샷 추가 하나만** 넣는 걸로 범위를
  좁혀서 [[옵션조합관리]](관리자용 옵션 그룹 관리 화면)는 만들지 않고
  가격(700원)을 코드 상수로 고정함(`EXTRA_SHOT_PRICE`). 메뉴 카드를
  클릭하면 바로 담기지 않고 옵션 선택 모달이 먼저 뜸. 같은 상품도
  옵션이 다르면 장바구니에서 별도 줄이어야 해서, 상품 `_id`와 별개로
  장바구니 전용 `cartItemId`(`makeCartItemId`)를 도입하고 수량 증감/
  삭제 로직을 전부 이 id 기준으로 바꿈(기존엔 상품 `_id`를 그대로
  키로 썼는데 더 이상 유일하지 않게 됨). 옵션 추가금은 장바구니에
  담는 시점에 이미 가격에 더해서 저장하므로, 총액 계산이나 매출 통계
  집계 로직은 손대지 않고도 자동으로 반영됨. `OrderItem`에
  `hasExtraShot`과 (원래 선언에 없었지만 Mongoose가 자동으로 부여하던)
  `_id`를 타입에 추가해 주문 내역/주방 화면에 "샷 추가" 표시와 안전한
  React key로 씀. 로컬에서 옵션 있는 주문 생성 → 조회까지 curl로
  검증(테스트 데이터 정리 완료)([[상품옵션선택]] 참고)
- [x] 옵션 모달 이미지 잘림 수정 + "마법의 주문" 옵션 추가 — 옵션
  모달의 사진이 가로로 넓은 틀(`h-40 w-full` + `object-cover`)에 눌려
  잘려 보이던 문제를, 상품 카드와 같은 3:4 비율을 유지한 채 **사진을
  왼쪽, 옵션을 오른쪽**에 두는 레이아웃으로 바꿔 해결함. 메이드 카페
  컨셉의 재미 옵션 "마법의 주문"(서빙 시 직원이 외쳐주는 주문 —
  "모에모에뀽"/"오이시쿠나레"/"냥냥쿵"/"하피네스차지!" 4항목, 드롭다운
  선택, 가격 영향 없음)을 `MAGIC_SPELL_OPTIONS` 상수로 추가하고
  `CartItem`/`OrderItem`에 `magicSpell` 필드로 반영. `makeCartItemId`가
  샷 추가 여부와 마법의 주문 선택값을 함께 조합해 장바구니 줄을
  구분하도록 확장(예: `"<id>:extra-shot:spell:모에모에뀽"`). 로컬에서
  두 옵션을 동시에 선택한 주문의 생성/조회를 curl로 검증(테스트
  데이터 정리 완료)([[상품옵션선택]] 참고)
- [x] 온도(HOT/ICE) 옵션 추가 — 가격 영향 없는 자유 선택으로,
  `TEMPERATURE_OPTIONS = ['HOT','ICE']` 상수와 함께 옵션 모달에 토글
  버튼 2개로 추가(이미 고른 걸 다시 누르면 선택 해제). 카테고리별로
  옵션을 다르게 보여주는 기능(예: 디저트엔 온도 옵션 숨기기)은 이번
  범위에 넣지 않아서, 모든 메뉴에 똑같이 노출됨. `CartItem`/
  `OrderItem`에 `temperature` 필드 추가하고 `makeCartItemId`가 온도까지
  포함해 장바구니 줄을 구분하도록 확장. 백엔드 `Order` 스키마의
  `items.temperature`에 `enum: ['HOT','ICE']` 검증을 걸어, 로컬에서
  유효하지 않은 값("WARM")을 보내면 400으로 거부되는 것까지 curl로
  확인. 세 옵션(온도+샷 추가+마법의 주문)을 모두 선택한 주문의 생성/
  조회도 함께 검증(테스트 데이터 정리 완료)([[상품옵션선택]] 참고)
- [x] 메뉴별 자유 옵션(옵션조합관리) 추가 — "메뉴를 추가할 때 옵션도
  추가할 수 있게 해달라"는 요청으로, 애초에 [[옵션조합관리]]가
  예상했던 "옵션 그룹을 별도 화면에서 관리하고 상품에 매핑"하는
  구조 대신, **메뉴 추가/수정 폼에서 그 메뉴만의 옵션(이름+추가금)을
  직접 추가/삭제**하는 더 단순한 형태로 범위를 좁혀 구현. `Product`에
  `options: { name, price }[]` 필드를 추가하고, 이름+추가금 입력 줄을
  다루는 `ProductOptionsEditor`를 `entities/product`에 만들어
  `AddProductForm`/`EditProductForm` 양쪽에서 재사용(수정 시엔 폼이
  옵션 목록 전체를 다시 보내므로, 백엔드가 항상 `options: options ?? []`
  로 덮어써서 지운 옵션이 실제로 삭제되게 함 — 재고 수량의 "생략하면
  유지" 방식과 다름). 고객 화면 옵션 모달엔 `product.options`가 있을
  때만 체크박스 목록이 추가로 뜨고, 선택한 옵션들은 `CartItem`/
  `OrderItem`의 `selectedOptions`(이름+가격 스냅샷)로 담겨 가격에
  합산되고 `cartItemId`/주문 내역·주방 화면 배지에도 반영됨. 로컬에서
  옵션 2개를 등록한 메뉴로 주문 생성 → 옵션 스냅샷 조회, 그리고 수정
  폼에서 옵션을 빈 배열로 저장했을 때 실제로 지워지는지까지 curl로
  검증(테스트 데이터 정리 완료)([[옵션조합관리]] 참고)
- [x] 주문 카드의 옵션 배지가 잘려 보이던 버그 수정 — 운영 DB의 실제
  주문(샷 추가+마법의 주문+온도를 모두 고른 주문)을 확인해 옵션 데이터
  자체는 정상 저장되는데 주방 화면에서 안 보인다는 신고를 조사한 끝에,
  `OrderCard`/`OrderSummary`가 메뉴 이름과 옵션 배지를 한 줄 `truncate`
  문단에 같이 넣어서 이름이 조금만 길어져도 뒤따르는 배지가 화면 밖으로
  통째로 잘렸던 게 원인이었음. 옵션 배지를 `flex-wrap`으로 줄바꿈되는
  별도 영역으로 분리해 해결. Vercel 배포 후 청크 안에 `flex-wrap`과
  `purple-100`(커스텀 옵션 배지 색)이 함께 들어있는지 확인해 실제
  반영을 검증(테스트 데이터 정리 완료)([[상품옵션선택]] 참고)
- [x] "샷 추가"를 매장 전체 고정 옵션에서 메뉴별 자유 옵션으로 이동 —
  "메뉴에서 샷 추가가 기본 옵션이 아니라 추가해야 하는 옵션이었으면
  좋겠다"는 요청으로, `ProductCard`에서 모든 메뉴에 자동으로 뜨던
  "샷 추가" 체크박스(+`EXTRA_SHOT_PRICE` 상수)를 완전히 제거하고, 관리자가
  필요한 메뉴에 [[옵션조합관리]]로 "샷 추가"(이름)+700원(추가금)을
  직접 등록하는 방식으로 통합. `hasExtraShot`/`EXTRA_SHOT_PRICE`가
  "새 카트에 담기" 경로에서 전부 죽은 코드가 되는 문제를 피하려고
  `CartItem.hasExtraShot`, 장바구니 스토어의 `hasExtraShot` 파라미터,
  `EXTRA_SHOT_PRICE` 상수까지 함께 삭제 — `OrderItem.hasExtraShot`과
  백엔드 스키마 필드, `OrderCard`의 표시 로직만 과거 주문 호환용으로
  남김. 로컬에서 "샷 추가"를 메뉴별 옵션으로 등록한 뒤 주문 생성 →
  `selectedOptions`로 정상 기록되는 것까지 curl로 검증(테스트 데이터
  정리 완료)([[옵션조합관리]] 참고)
- [x] 운영 DB 주문 내역 전체 삭제 — 사용자가 명시적으로 요청해 운영
  Order 컬렉션의 실제 주문 36건(2025-07-27 ~ 요청 시점)을 되돌릴 수
  없는 영구 삭제로 진행. 파괴적 작업이라 `AskUserQuestion`으로 건수와
  "백업 없음"을 명시하고 명확한 확인을 받은 뒤에만 실행. 주문 삭제
  API가 없어 임시 `wipe-orders.mjs` 스크립트(mongoose 직접 연결,
  `deleteMany({})`)로 처리하고 즉시 스크립트 파일을 삭제. 삭제 후
  `GET /api/orders`로 0건임을 재확인. 판매 통계도 이 시점부터 전부
  0으로 초기화됨
- [x] "온도(HOT/ICE)"를 매장 전체 고정 옵션에서 메뉴별 자유 옵션으로
  이동 — "샷 추가"와 똑같은 이유("메뉴에서 기본 옵션이 아니라
  추가해야 하는 옵션이었으면 좋겠다")로, `ProductCard`에서 모든
  메뉴에 자동으로 뜨던 "온도" 토글 버튼 2개(`TEMPERATURE_OPTIONS`
  상수)를 완전히 제거하고, 관리자가 필요한 메뉴에 [[옵션조합관리]]로
  "HOT"/"ICE"(이름)+0원(추가금, 보통)을 직접 등록하는 방식으로
  통합. `CartItem.temperature`, 장바구니 스토어의 `temperature`
  파라미터, `TEMPERATURE_OPTIONS`/`Temperature` 타입까지 함께
  삭제 — `OrderItem.temperature`와 백엔드 스키마 필드, `OrderCard`의
  표시 로직만 과거 주문 호환용으로 남김. 다만 온도는 원래 HOT/ICE
  중 하나만 고르는 배타적 토글이었는데, 일반 커스텀 옵션(독립
  체크박스)으로 옮기면서 그 배타성이 사라짐 — 관리자가 "HOT"과
  "ICE"를 모두 등록한 메뉴에서는 이론상 둘 다 체크될 수 있고, 이번
  범위에서는 이를 막는 로직을 추가하지 않음. 로컬에서 "HOT"/"ICE"를
  0원 커스텀 옵션으로 등록한 뒤 주문 생성 → `selectedOptions`로 정상
  기록되는 것까지 curl로 검증(테스트 데이터 정리 완료). 이 시점에는
  운영 DB에 과거 주문이 이미 전부 삭제된 상태라 호환 경로를 탈 실제
  데이터는 없음([[옵션조합관리]] 참고)
- [x] "마법의 주문"을 매장 전체 고정 옵션에서 메뉴별 자유 옵션으로
  이동 — 샷 추가/온도와 같은 이유로 마지막 남은 매장 전체 고정
  옵션까지 옮기면서, `ProductCard`의 옵션 모달에서 매장 전체 고정
  옵션 UI가 완전히 사라짐(이제 `product.options` 체크박스만 남음).
  `MAGIC_SPELL_OPTIONS` 상수가 유일한 export였던
  `entities/product/model/optionConstants.ts` 파일 자체를 삭제(빈
  파일을 남겨두지 않기 위해). `CartItem.magicSpell`, 장바구니 스토어의
  `magicSpell` 파라미터도 함께 삭제 — `OrderItem.magicSpell`과 백엔드
  스키마 필드, `OrderCard`의 표시 로직만 과거 주문 호환용으로 남김.
  이제 세 옵션(샷 추가/온도/마법의 주문) 모두 매장 전체 고정 옵션에서
  메뉴별 자유 옵션으로 이동을 마쳐서, [[상품옵션선택]] 문서는 실제
  동작 설명 역할을 [[옵션조합관리]]에 넘기고 히스토리/과거 데이터
  호환 설명 문서로 성격이 바뀜. 로컬에서 "모에모에뀽"을 0원 커스텀
  옵션으로 등록한 뒤 주문 생성 → `selectedOptions`로 정상 기록되는
  것까지 curl로 검증(테스트 데이터 정리 완료)([[옵션조합관리]] 참고)
- [x] 메뉴 옵션 폼에 "자주 쓰는 옵션" 원클릭 추가 칩 — 세 옵션(샷
  추가/온도/마법의 주문)이 매장 전체 고정에서 메뉴별 자유 옵션으로
  전부 옮겨가면서, 관리자가 이 옵션들을 쓰고 싶은 메뉴마다 이름/가격을
  매번 직접 타이핑해야 하는 불편이 생김 — "메뉴 추가/수정할 때 HOT/ICE,
  마법의 주문 등 옵션들을 전부 추가·수정할 수 있게 해달라"는 요청으로,
  `ProductOptionsEditor`에 `PRESET_OPTIONS`(샷 추가 +700원, HOT/ICE,
  모에모에뀽/오이시쿠나레/냥냥쿵/하피네스차지! — 전부 예전 고정
  옵션이었던 값 그대로) 목록을 원클릭 칩 버튼으로 추가. 이미 추가된
  옵션은 칩 목록에서 걸러내 중복 추가를 막음. 새 데이터 모델이나
  백엔드 변경 없이 기존 `Product.options`(이름+가격) 그대로 재사용
  ([[옵션조합관리]] 참고)

## 다음 우선순위 로드맵

아래 항목 중 실제 작업을 시작하는 항목은 `docs/AGENTS.md`의 SDD 워크플로우에
따라 LeanSpec으로 개별 스펙(`docs/specs/`)을 만들고 진행 상태를 추적합니다.
이 문서는 우선순위 개요만 담고, 항목별 상세 설계/작업 분해는 해당 스펙에
기록합니다.

1. **결제 연동 여부 결정** (최우선) — 주문 생성까지만 구현되어 있어, 실제
   결제 수단을 붙일지 매장에서 별도 결제할지 먼저 결정이 필요합니다.
2. **주문 상태 흐름 설계** — 접수/준비중/완료 등 상태 전이가 필요한지
   검토합니다.
3. **테스트 도입** — 현재 프론트엔드/백엔드 모두 자동화된 테스트가
   없습니다. 주문/재고 관련 로직부터 우선 커버합니다.
4. **배포 파이프라인 정비** — 배포 대상은 Vercel(frontend) / Render
   (backend, 무료 티어)로 전환 완료했고 슬립 방지용 헬스체크(`GET
   /health`)도 붙어 있습니다. 운영 환경변수(`NEXT_PUBLIC_API_URL`,
   `MONGO_URI`, `R2_ENDPOINT`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`,
   `R2_BUCKET_NAME`, `R2_PUBLIC_URL`, `ADMIN_PASSWORD`, `SESSION_SECRET`,
   `FRONTEND_ORIGIN`)는 2026-09-07에 Render에 전부 등록해 운영 환경에서도
   관리자 로그인이 정상 동작함을 확인했습니다([[관리자인증인가]],
   `docs/specs/001-admin-auth` 참고). 남은 일은 프론트/백엔드 CI
   연동입니다 (현재 `.github/workflows/`에는 `docs/` Quartz 배포
   워크플로만 있음).
   > ⚠️ `FRONTEND_ORIGIN`은 트레일링 슬래시 없이 정확한 오리진만
   > 넣어야 합니다(`https://.../app` ✅, `https://.../app/` ❌) — `cors`
   > 패키지가 값을 그대로 `Access-Control-Allow-Origin`에 반영할 뿐
   > 정규화하지 않아서, 슬래시 하나 차이로도 브라우저가 CORS를 막습니다.
   > ⚠️ Render 같이 앞단에 프록시가 있는 배포 환경에서는
   > `app.set('trust proxy', 1)`이 없으면 `secure: true`인 쿠키(세션
   > 등)가 아예 발급되지 않습니다. 새 배포 대상을 추가할 때마다 이
   > 설정이 있는지 확인하세요.
5. **Tailwind 설정 정리** — 루트 `tailwind.config.ts`가 앱별 공용 설정
   패키지(`packages/config`) 없이 단독으로 존재합니다. 앱이 늘어나면
   통합을 검토합니다.
6. **관리자 역할 구분/감사 로그 검토** — 현재는 공유 비밀번호 하나로만
   인증하며 점주/직원 구분이나 조작 이력이 없습니다([[관리자인증인가]]
   참고). 필요해지면 별도 스펙으로 착수합니다.

## 완료 기준 (Definition of Done)

- `pnpm check-types`, `pnpm build`(또는 `turbo run check-types build`)가
  모두 통과할 것
- 새/변경 파일에 `@owner: ai` 또는 `@owner: human` 태그가 붙어 있을 것
- 외부와 통신하는 함수(REST 호출, API 라우트)에는 TSDoc으로 무엇을
  주고받는지 명시할 것
- UI 변경은 실제 화면(dev 서버)에서 정상 동작을 확인할 것

## 관련 문서
- [[기획서]] — 이 계획이 구현하는 기능 범위와 사용자 흐름
